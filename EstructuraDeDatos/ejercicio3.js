/* Construir una función que sume los elementos de una lista de enteros recursivamente */

// declaro una variable de inicio
let inicio = 0;

// decalro otra variable que sea el fin del arreglo
let fin = 6;

// recoro el arreglo sumando los numeros
for (let num = 1; num<=fin; num++) {
    inicio = inicio + num
    
}

console.log(inicio)

